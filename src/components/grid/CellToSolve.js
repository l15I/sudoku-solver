/** @jsx jsx */
import { css, jsx } from '@emotion/core'

export default function (props) {
  const styles = [css`
    margin: auto;
    font-size: .8em;
  `]

  return <div css={styles}>
    to solve
  </div>
}